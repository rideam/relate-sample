title: "Linear Algebra"
description: |
    # Linear Algebra Recap

    - Laws of indices
    - Vectors

rules:
    access:
    -
        if_has_role: [student, ta, instructor]
        permissions: [view]

    grade_identifier: null

pages:

-
    type: Page
    id: intro
    content: |

        # Laws of Indices 

        {% from "content-macros.jinja" import indented_include %}
        {{ indented_include("markdown/indices.md", 0) }}

        Yadda yadda.

        {% from "content-macros.jinja" import youtube %}
        {{ youtube("2yJgwwDcgV8") }}

-
    type: Page
    id: vectors
    content: |

        # Vectors

        A vector is pretty straight.


completion_text: |

    # End of Linear Algebra Recap

    Now you're ready to take the [quiz](flow:quiz-test).
